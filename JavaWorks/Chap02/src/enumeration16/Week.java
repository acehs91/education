package enumeration16;

/*
     열거 타입 선언 = 첫글자는 대문자, 열거 파일명과 동일하게 선언
     한정된 값을 열거 상수로 정의 => 관습적으로 대문자, 단어+단어시_ (언더바) 로 연결
     (예시)
     public enum LoginResult 
 */


public enum Week {
   MONDAY,
   TUESDAY,
   WEDNEDAY,
   THURSDAY,
   FRIDAY,
   SATURDAY,
   SUNDAY,
}
